# M12 — Carry-role usage

Generated 2026-09-08.

> No current game player ratings were used to fit the nflverse baseline.

## model name

M12 — Carry-role usage

## target definition

Historical designed-carry counts by `rusher_player_id` per team-season, ranked into ROLE_1..ROLE_3 + ROLE_OTHER; the value is each role's share of team designed carries.

## eligible population

§6.1 core + §6.5 REG, then `rush_attempt == 1`, `qb_scramble == 0`, `qb_kneel == 0`, `rusher_player_id` present.

## row count by season

39,152 events across 96 team-seasons ([2023, 2024, 2025]).

## missingness

`rusher_player_id` must be non-null (that IS the target). Sliced contexts with < 500 events or team-seasons with too few events are dropped from that slice.

## class distribution / target distribution

| context | ROLE_1 / ROLE_2 / ROLE_3 / ROLE_OTHER (shrunk mean share) |
| --- | --- |
| overall=all | 0.547 / 0.241 / 0.092 / 0.120 |
| distance=normal_3_plus | 0.563 / 0.244 / 0.090 / 0.103 |
| distance=short_1_2 | 0.482 / 0.251 / 0.122 / 0.145 |
| field=open_field | 0.551 / 0.242 / 0.091 / 0.116 |
| field=red_zone | 0.541 / 0.239 / 0.101 / 0.120 |
| game_state=late_lead | 0.507 / 0.242 / 0.133 / 0.125 |
| game_state=late_trail | 0.499 / 0.253 / 0.135 / 0.119 |
| goal_to_go=gtg | 0.560 / 0.247 / 0.101 / 0.098 |

## predictor list

No fitted predictors — this is an empirical role-share table. Conditioning dimensions: distance, field, game_state, goal_to_go, overall.

## forbidden-variable check

No player ratings, no `overall`, no downstream outcomes. Role rank is by historical event volume within a team-season only (spec §15 usage≠ability).

## baseline model

Rank players by carry volume within each team-season → ROLE_1..3, ROLE_OTHER. Share per role per team-season, empirical-Bayes shrunk toward the league share (pseudo-count k=60.0).

## challenger models

None — a role-share prior, not a classifier.

## 2024 validation metrics

N/A (no fit). Role-share means are pooled over 2023–2025; the sd / p10 / p90 columns in `artifacts/distributions/carry_role_shares.parquet` give the team-to-team spread the engine can sample.

## 2025 locked test metrics

N/A — see distribution parquet for spread.

## calibration plots

Mean role-share concentration (Herfindahl of the overall shares across team-seasons): 0.408.

## important conditional diagnostics

Situational rows above show how the lead role's share moves in the red zone / short yardage / third down / two-minute / late-lead states relative to `overall=all`.

## historical residual variance estimates

The `sd_share` / `p10_share` / `p90_share` columns per role ARE the historical team-to-team spread; the engine adds a per-team draw from these when assigning roles.

## final chosen model

Empirical shrunk role-share table → `artifacts/distributions/carry_role_shares.parquet`.

## production refit metadata

Built on [2023, 2024, 2025]; 96 team-seasons, 39,152 events. Regenerate by re-running the script.
